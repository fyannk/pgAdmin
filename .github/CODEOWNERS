# Every pull request gets @fyannk as a reviewer automatically.
#
# The repository rule on main requires zero approvals, so this does not gate
# merging; it exists so review is requested without anyone having to remember
# to ask, and so the owner of each area is written down rather than known.
*                           @fyannk

# The image recipes. Everything this project actually ships is decided here:
# what the runtime contains, what privileges it can hold, and which pgAdmin
# release it corresponds to.
/Dockerfile                 @fyannk
/Dockerfile.upstream        @fyannk

# Publishing and supply chain: anything here decides what gets built,
# attested, and pushed under the project's name.
/.github/workflows/         @fyannk
/LICENSE                    @fyannk
